# PySide 正式发布清单

发布事实由每次构建自动生成的 `dist-pyside/美客多活动助手/release-manifest.json` 记录，不在文档中保存会过期的文件数、大小或哈希。

清单包含：产品标识、协议版本、构建指纹、文件数、目录总大小、主程序长度与 SHA256、Node 版本与 SHA256。安装器会在覆盖前后重新核验这些字段。

正式发布物始终是完整 one-dir 目录，不能只复制主 EXE。旧 WinForms 安装版保留为显式回退版本。
