# Codex 项目规则

## 1. 全局规则引用

本项目默认遵守用户全局自定义指令。本文件只记录项目专属事实、项目级流程和项目级补充，不重复全局工作流规则。

新建空项目时，本文件可以保留待填写项。不要凭空编造技术栈、命令、目录结构或验证方式；只填写已经明确或能从项目文件中验证的信息。项目初始化后，可再补全本文件。

本模板应保持跨平台中立。只有当前项目真实存在且需要执行的项目路径、命令或环境信息，才写入生成后的项目 `AGENTS.md`；不要把某台设备的规则源安装路径、全局模板路径或 workflow skill 路径当成所有项目默认事实写入模板。

### 模板同步信息

- 模板来源：本机项目级规则模板 `AGENTS.template.md`
- 模板版本/提交：
- 模板校验：8E50CC5F8C0AF7B72DED163C225E6332E361B2B96F106A23988B665818977AED
- 上次同步时间：2026-07-06
- 同步方式：结构化合并
- 已确认保留的项目专属覆盖：无
- 待确认模板冲突：无
- 同步备注：按当前项目 AGENTS.md 与最新模板结构化比较；当前项目未填写项目专属内容

## 2. 项目概览

- 项目名称：
- 项目背景：
- 服务对象：
- 核心问题：
- 主要交付物：

## 3. 当前状态

- 当前阶段：待填写（需求讨论 / 技术选型 / 开发 / 修改 / 测试 / UI 调整 / 打包发布 / 维护）
- 当前主线任务：
- 当前阻塞：
- 下一步：
- 最近一次有效结论：

## 4. 需求与范围

- 已确认需求：
- 暂不做：
- 待确认问题：
- 验收口径：
- 需求变更规则：需求、范围、验收标准发生变化时，必须先说明影响，再等待用户确认。
- 范围核对规则：执行项目任务、规则同步或线程下派前，必须核对用户原话中的对象、范围、边界和禁止项；不得把用户要求从 A 扩大成 A+B。任何范围扩展都必须先说明影响并等待用户确认。

## 5. 技术栈与运行环境

- 前端：
- 后端：
- 数据库：
- 包管理器：
- 运行环境：
- 主要框架/库：
- 外部服务：
- 关键依赖：

未知项保持空白或标注待确认；不得凭空填写。

## 6. 常用命令

- 安装依赖：
- 启动开发：
- 运行测试：
- 构建：
- 代码检查：
- 格式化：
- 数据库/迁移：
- 打包/发布：

命令必须来自项目文件、README、脚本配置或用户确认；不能猜测。缺少命令时，先标注待确认。

## 7. 目录结构与关键文件

- `src/`：
- `tests/`：
- `docs/`：
- `scripts/`：
- 配置文件：
- 入口文件：
- 数据文件：
- 生成文件：
- 其他关键目录：

如果项目结构不同，应按真实结构调整本节。

## 8. 项目边界

- 不要修改：
- 修改前必须确认：
- 不能删除：
- 不能自动提交/推送：
- 不能自动发布/保存/上传：
- 敏感文件或目录：
- 生产环境或外部系统限制：
- 其他项目专属限制：

## 9. 开发与修改流程

适用于编程、修 bug、改功能、改配置、改文档、改 UI、接入服务、排查问题等项目任务。

1. 先确认目标、范围、影响文件、风险和验证方式。
2. 读取项目事实来源，例如 README、配置文件、脚本、目录结构、现有代码和测试。
3. 形成完整方案后再执行，不按零散猜测逐步乱改。
4. 默认采用外科手术式修改：先定位最小故障面，只修改已确认错误和必要依赖点，不因相邻、相似、风格不统一或主观认为更好而顺手修改正常相关内容。
5. 修改前必须同时做大局校验：判断问题是否只是表象，是否暴露系统性缺陷、重复逻辑、架构不一致、流程漏洞、规则冲突，或会在其他模块、入口、项目、后续阶段再次出现。
6. 执行前必须列出修改边界：允许修改什么、禁止修改什么、哪些疑似风险只观察不修改、什么条件下才升级为范围化修复。
7. 如果证据表明局部修复会掩盖根因、制造补丁堆积、破坏一致性或导致后续更大问题，必须提出范围化修复方案；方案应说明为什么局部修复不够、涉及范围、必须一起改的内容、仍然不应改的内容、风险、回滚方式和验证矩阵，并等待确认。
8. 未经确认，不得把“大局考虑”变成顺手重构、风格统一、扩大范围或重写。
9. 如果发现需求不清、依赖缺失、环境不一致、测试不可用、权限不足或外部服务异常，先诊断并提出处理方案。
10. 修改后必须根据改动范围运行或说明对应验证，并输出边界回检：实际修改了什么、未修改什么、是否触发范围化修复条件、是否存在计划外改动、验证是否覆盖修改边界。
11. 修改后必须回读或对比改动，确认没有无关重构、无关格式化、无关命名变化、无关结构调整或既有正常行为变化；如发现计划外改动，必须说明原因并请求确认保留、回滚或继续修正。
12. 当同一任务中出现重新生成、改方向、缩小范围、替换输出，或用户指出“不用”“不是这个意思”“只要”“重新来”等情况时，必须主动检查本任务中此前由 Codex 生成的文件、目录和临时产物；若旧产物已被新要求替代，必须列出疑似废弃清单并询问是否删除。
13. 当交付物因位置、文件名、格式、内容、版本或用户要求发生重新生成、重新打包、重新导出、复制到正式位置，或从临时位置转移到目标位置时，必须主动识别旧交付物、临时包、中间产物和错误位置文件；若它们不再是最终交付物，必须列出清单并询问是否删除、归档或保留，避免用户误用旧文件。
14. 完成汇报必须包含修改内容、验证结果、未验证项、疑似废弃产物处理情况和剩余风险。
15. 面向用户的说明、汇报、文档、规则、标题、按钮文案、字段说明或界面文字中，如果出现英文术语、英文短语、英文缩写或英文文件名含义，必须在后面补充中文解释或翻译；代码标识符、命令、路径、文件名、包名、环境变量、API 字段、日志原文、报错原文、配置键名等保持原样，必要时单独补充中文说明。

## 10. 验证要求

修改后必须根据改动范围运行对应检查。

- 代码修改后：
- UI 修改后：
- 数据/配置修改后：
- 文档修改后：
- 外部系统修改后：
- 打包/发布前：

如果检查失败，先诊断原因并提出修复方案，不能跳过或降低标准。

如果验证命令缺失、依赖缺失或运行环境不可用，应记录为环境/依赖问题，并按全局规则处理；不能默认换工具链或把替代验证当成正式验证。

### 用户可见界面规则

启用条件：当项目交付物包含桌面软件、Web 前端、工作台、管理后台、设置页、历史页、报表页或其他用户可见界面时，启用本节。

- 用户可见界面默认只展示业务操作、业务状态、业务结果和用户需要决策的信息。
- 不得把后端实现细节、内部数据设置、审计目录、导出目录、缓存路径、JSONL、raw response、数据库字段、接口字段、内部 jobId/runId、技术状态码、token 相关字段或调试参数直接暴露在日常界面中。
- 读取并发、活动并发、写入并发、重试次数、分页大小、缓存刷新策略等技术参数默认不得作为日常设置项要求用户理解或维护；若必须保留，应放入“高级 / 诊断 / 排障”区域，并说明仅用于排障、压测或主管要求。
- 内部目录如 exports、logs、审计文件目录、诊断文件目录可以保留为程序内部能力，但不得命名成会误导用户日常查看或操作的“输出目录”；如需暴露，必须使用“诊断文件目录 / 内部记录目录”等明确文案。
- 主表、设置页、历史页和汇总页应优先使用业务口径，例如店铺、站点、活动、商品数、成功、失败、跳过、业务失败原因和处理建议；内部证据应进入详情、复制详情或诊断入口。
- UI 修改后必须回检：日常界面是否仍暴露不必要的后端字段、内部目录、审计路径、调试参数或技术缩写；如有保留，必须说明为什么用户需要看到以及为何不放入高级诊断区。

## 11. 批量操作与业务质量门控

启用条件：当任务涉及内容生成、表单填写、配置生成、数据写入、批量保存、发布、提交、上传、外部系统修改或其他高风险自动化时，启用本节。未涉及时，说明不适用即可。

项目专属配置：

- 批量对象类型：
- 保存/发布/写入目标：
- 业务验收项：
- 抽检方式：
- 硬阻断条件：
- 允许继续自动化的低风险异常：

通用规则：

- 不能只以“字段存在、格式合格、接口成功、保存成功、命令退出码为 0”判定完成。
- 必须根据用户目标、项目事实和本节配置定义业务验收项。
- 业务验收项应覆盖：内容语义、格式要求、跨字段一致性、未授权信息、旧内容残留、用户明确禁用项。
- 如果用户提供固定模板、固定格式、固定口径或固定示例，候选生成和验收都必须逐项对齐，不得使用旧模板、泛化模板或自行改写结构。
- 模板占位符、说明语、内部标记或待确认提示不得原样写入用户可见字段。
- 内容生成、清洗、改写、本地化或格式化不得牺牲已证实的事实完整度。
- 批量业务验收可以使用临时只读分片子任务提效，但分片必须收到完整验收任务包，并严格遵守只读边界、禁止事项、验收清单和统一回传格式。
- 临时验收分片结论只作为主验收线程、主管线程或汇总任务的输入，不得单独放行对象、批次或阶段。
- 分片汇总不能用平均或多数投票判定通过；必须合并所有分片问题并统计全局同类问题。任一硬阻断对象不得放行；同类问题达到项目定义阈值时，必须判定为系统性失败并触发返工或暂停。
- 批量保存、发布、写入、提交或外部系统修改完成后，应按风险抽样回读实际结果，确认最终展示、存储或系统状态与预期一致。
- 抽检样本应覆盖关键字段、边界情况和高风险类型。
- 如果操作后抽检发现业务质量问题、数据未落库、页面错位、旧内容残留、内容变形、跨字段冲突或用户禁用项，必须暂停后续阶段，并触发返工、回滚或修复方案。
- 完成汇报必须包含业务质量验收和操作后抽检证据；不能只报告“已保存”“接口成功”“执行完成”。

## 12. 人工审核队列

启用条件：遇到失败、低置信结果、缺少事实、权限限制、账号状态、验证码、安全合规、外部系统异常、批量质量疑点或需要用户判断的问题时，启用本节。

项目专属配置：

- 人工审核项格式：
- 阻塞级别：
- 汇总位置：
- 需要用户选择的选项格式：

通用规则：

- 先判断问题是否会阻塞后续自动化或影响结果安全。
- 若不影响安全、数据正确性和后续步骤，可记录为待人工审核项，保留原因、证据、影响范围和建议选项，并继续处理其他可自动完成的低风险任务。
- 待人工审核项应在阶段末尾或任务末尾统一汇总请求用户处理，避免频繁打断自动化。
- 若该问题会影响后续判断、导致错误结果、数据损坏、越权操作、不可逆修改、误删、误提交、误保存、误上传，或涉及账号、权限、验证码、安全合规等高风险事项，必须立即停止并请求确认。
- 不能为了不中断自动化而跳过关键阻塞。

## 13. 线程协作规则

启用条件：当项目明确使用主管线程、规则线程、执行线程、测试线程、验收线程、UI 线程、打包线程或其他子线程协作时，启用本节。未启用多线程协作时，本节可保持未配置。

项目专属配置：

- 主管线程名称：
- 主管线程 ID：
- 规则线程名称：
- 规则线程 ID：
- 常用子线程类型：
- 主线程可直接处理的任务类型：
- 必须下派的任务类型：
- 子线程任务包格式：
- 子线程 ACK 格式：
- 子线程回传格式：
- 下派任务台账位置：
- 未确认启动处理方式：

通用规则：

- 主管线程负责项目总控、阶段拆分、任务下派、决策记录、验收标准和子线程结论汇总。
- 当某线程被明确设定为项目主管线程、主线程、总控线程或项目协调线程后，该线程默认只负责总控，不直接执行具体生产任务。
- 主管线程职责包括需求澄清、总体规划、阶段拆分、任务优先级、验收标准、任务派发、风险判断、决策记录、子线程回传汇总和下一步调度。
- 主管线程默认不得直接执行开发实现、批量修改、测试验证、UI 调整、图标/素材制作、打包发布、接口调试、依赖环境修复、故障排查、数据清洗、批量保存或外部系统操作。
- 除非用户明确说“主线程直接执行”，或该任务只是低风险只读分析、规划、汇总、规则整理，否则主管线程不应代替子线程干活。
- 当任务进入具体执行阶段，主管线程必须先判断是否需要下派；若任务会产生大量日志、截图、代码片段、失败尝试或临时判断，或需要独立实现、独立排查、独立验证、多轮讨论，或属于开发、测试、UI、打包、排障、接口、数据、素材、批量操作等明确执行角色，应优先下派到子线程。
- 主管下派任务时，必须明确线程名、任务边界、输入信息、完成标准和回传内容。
- 主管下派任务包必须包含线程名称、任务目标、任务边界、输入上下文、禁止事项、可执行动作、需要保留的证据、完成标准、验证方式、回传格式、是否允许扩大范围。
- 创建、命名或下派子线程时，如果用户在引号中给出线程名，必须只提取引号内的内容作为线程名，并原样使用。不得把整句话作为线程名，不得增删、扩写、翻译、改写、补充前后缀或替换同义词。例如：新建一个“UI”的线程，线程名必须是 UI；创建一个“测试”线程，线程名必须是 测试；开一个“打包发布”子线程，线程名必须是 打包发布。线程名只用于左侧栏识别和主管调度，完整任务说明必须写入任务包。
- 主管线程发送或创建子线程任务后，不得只凭“消息已发送”“已引导对话”或“已创建线程”认定子线程已经启动；主管必须建立或更新任务台账，至少记录线程名、线程 ID、任务名、发送时间、是否收到 ACK、当前状态和下一步处理。
- 子线程收到任务后的首轮有效回复必须先给 ACK，格式至少包含线程名、任务名、已收到、是否开始执行、预计回传内容；没有 ACK 的任务只能标记为“已发送 / 未确认启动”，不能标记为“执行中”或“等待结果”。
- 如果出现 `stream disconnected before completion`、发送失败、线程无响应、没有首轮 ACK、ACK 前中断、或无法确认子线程是否已收到任务，主管必须把该子线程标记为“下派未确认”或“状态未知”，并主动追问状态、重发任务、改派新线程或请求用户处理；不得默认子线程正在运行，也不得把无 ACK 的子线程计入已下派完成。
- 如果子线程已经 ACK 但后续断流，主管应标记为“执行状态未知”，先追问状态或要求回传阶段结果；如果没有完整回传包，不得判定任务完成。
- 主管创建或下派任何新子线程时，任务包必须包含本项目的线程协作要求，不得省略或弱化。
- 在线程协作任务开始时，子线程必须先主动发现线程工具。应优先通过 tool_search 精确搜索 `send_message_to_thread read_thread list_threads create_thread`。只有确认没有可用发送工具，或发送工具调用失败后，才允许使用手动回传包 fallback。不能在未搜索线程工具前直接判断“无法回传主管”。
- 子线程回传主管前，必须确认主管线程 ID 已提供且格式有效；如缺少主管线程 ID，应先请求用户或主管补齐，不得把“缺少线程 ID”误判为线程工具不可用。
- 子线程只处理被授权的子任务，不得扩大范围、跳到下一阶段、替主管做项目决策，或把临时失败方案沉淀为长期规则。
- 子线程发现范围变化、关键阻塞、验证失败、需要用户确认或需要主管决策时，必须停止并回传主管。
- 为提高只读验收效率而使用临时验收分片子任务时，分片默认不登记为长期项目线程；未经用户确认，不得自动创建长期分片线程。
- 临时验收分片任务必须明确只读边界、禁止事项、完整验收清单和统一回传格式；缺少这些内容时，分片结果不得作为放行依据。
- 项目内任何子线程出现关键状态变化时，都必须唤醒主管线程；关键状态包括：完成任务、完成阶段、完成检查、完成修改、完成排障、进入等待用户确认、遇到阻塞、执行失败、发现需要改变任务范围。
- 如果子线程可用线程工具，必须在最终回复前主动向主管线程发送回传消息。
- 回传消息必须包含：线程名、任务/阶段名称、当前状态、是否需要主管动作、简洁结论、关键证据、执行/修改内容、验证结果、未解决风险、建议下一步。
- 子线程回传不得夹带无关排障过程、临时失败尝试、已废弃方案、大段日志或未确认猜测；这些内容只保留在子线程本地，必要时用证据摘要引用。
- 只有主管唤醒消息发送成功后，子线程才能在本线程回复“已回传主管”。
- 如果发送失败，必须报告具体错误，不得声称已唤醒主管。
- 子线程不得把“在本线程输出总结”当作“已唤醒主管”。
- 子线程唤醒主管后，不得自行进入下一阶段，必须等待主管汇总并下派后续任务。
- 如果子线程无法使用线程工具，必须在本线程输出完整回传包，并提示用户使用“【线程名】完成，交给主管汇总”触发主管。
- 主管线程收到回传后，只吸收结论、证据、风险和决策项，不吸收子线程完整过程；主管线程必须更新项目状态，并决定下一步是继续下派、补充信息、暂停确认、重新验证、进入下一阶段还是结束阶段。
- 规则线程同步或修复线程协作机制时，应同步给主管、规则和全部相关子线程，并要求各线程回执确认已更新执行方式。
- 未经用户确认，不得自动创建新线程、读取无关线程或跨项目执行操作。

## 14. 规则筛流与项目进化

启用条件：当项目过程中出现可复用经验、重复问题、误解风险、流程断裂、验证遗漏、规则冲突、规则过长、规则落点不清或项目专属流程稳定成型时，启用本节。

项目专属配置：

- 本项目规则维护位置：
- 规则更新触发语：
- 规则审核人：
- 不应写入本项目规则的内容：

通用规则：

- 当前项目专属经验沉淀到本文件。
- 当前项目规则应按轻量四层归类维护：项目事实层、项目流程层、项目约束层、项目经验层。
- 项目事实层记录客观可验证的信息，例如项目目标、技术栈、运行环境、常用命令、目录结构、关键文件和外部服务。
- 项目流程层记录本项目稳定做事方式，例如开发、测试、打包、批量处理、人工审核、线程协作、发布或交接流程。
- 项目约束层记录不能做什么和高风险边界，例如不能删除/修改的文件、账号和数据边界、保存/发布限制、必须确认的项目专属操作。
- 项目经验层记录本项目踩坑、反复问题、稳定经验和项目级规则进化；成熟后再判断是否上升到全局项目模板或全局 workflow skill。
- 项目规则不会自动静默成长。Codex 只能主动发现事实、流程、约束或经验并提出沉淀建议；建议必须说明写入哪一层、原因、预期效果和副作用，等待用户确认后才能写入。
- 所有未来新项目都应该默认具备的项目规则、栏目或结构，建议沉淀到全局项目模板。
- 所有项目都通用的行为规则，建议沉淀到全局自定义指令或全局 workflow skill。
- 复杂、可复用的执行流程，建议沉淀到全局 workflow skill。
- 同时适用于 macOS 和 Windows 的长期规则，应先沉淀到 GitHub 规则源，再由各设备安装脚本同步到本机；不要只改某个项目或某台机器的安装副本。
- 临时、一次性、尚未验证的经验，只作为当前任务提醒，不写入长期规则。
- 修改规则前必须先提出规则进化建议，说明触发场景、问题原因、建议内容、预期效果、验证方式、接受标准、可能副作用和建议落地位置，并等待用户确认。
- 修改项目规则前应优先小范围 add/delete/replace；如需大段重写，必须说明为什么局部修改不够。
- 修改后应回读检查是否解决原问题，并确认没有重复、冲突、跑偏、过度约束或上下文膨胀。
- 每次新增、删除、修改或合并规则后，必须做整体规则整理检查，不能只验证“新增规则已写入”。
- 规则整理检查必须确认：规则放置位置正确，阶段顺序和线程职责顺畅，表达不重复、不冲突、不繁杂，没有保留被新规则替代的旧口径，每条规则可被对应执行线程或验收线程实际检查。
- 规则整理检查必须包含关键词搜索和相关段落回读；验证范围至少覆盖新增规则、相邻规则、可能冲突的旧规则、回传格式、验收口径和模板沉淀判断。
- 每次沉淀规则时必须判断适用范围：只适用于当前项目的写入当前 `AGENTS.md`；以后新项目也应默认具备的规则、栏目或结构，建议同步蒸馏到全局项目模板；所有项目通用行为规则再建议沉淀到全局自定义指令或全局 skill。
- 如果判断不写入项目级模板，必须说明原因；如果写入模板，必须回读验证模板没有重复、冲突、跑偏或过度约束。

## 15. 完成标准

- 功能/内容已完成：
- 必要验证已通过：
- 业务质量门控已完成或说明不适用：
- 待人工审核项已汇总：
- 线程回传已完成或说明不适用：
- 疑似废弃产物已清点并处理或等待确认：
- 用户确认项已处理：
- 未验证项已说明：
- 剩余风险已说明：
- 下一步建议：

## 16. 项目专属补充

本节用于记录只属于当前项目的规则、业务边界、特殊流程、外部系统限制、验收口径或用户偏好。

- 项目专属规则：
- 项目专属风险：
- 项目专属验收：
- 项目专属交接：
